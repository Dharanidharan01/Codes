
// Step 5: Click the "Switch to your time" toggle button
    cy.get('button.event-time-timezone-toggle_toggle', { timeout: 10000 })
      .should('be.visible')
      .click()

    // Step 6: Click the "Add to Calendar" button
    cy.get('a.cosmos-button[href*=".ics"]', { timeout: 10000 })
      .should('be.visible')
      .click()
