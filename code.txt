
if (itemName in this.#menu) {
    let price = this.#menu[itemName];
    let cost = price * quantity;

    // Add the order to the list
    this.#order.push({ itemName, quantity, cost });

    // Update the total bill
    this.#total += cost;

    console.log(`Ordered ${quantity} x ${itemName} = ₹${cost}`);
  } else {
    console.log(`${itemName} is not available in the menu.`);
  }
}
